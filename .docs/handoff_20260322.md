# Handoff Note (2026-03-22)

## 1. 目的と現状
- 目的: GIVE系コンペのベースラインをまず提出できる状態にする
- 現状: `scripts/run.sh` は実行成功済み（ただし VACE重み未配置のため identity fallback で出力）
- 直近の成功 run: `logs/20260321_192227/output.mp4`

## 2. 今回の試行サマリ
- `data/default/train/0000.parquet` を調査
  - 行数: 100
  - 列: `video` のみ
  - 各行は dict で `bytes`, `path` を保持
- `pyarrow` 未導入で parquet 読み込み失敗 → `/usr/bin/python3 -m pip install pyarrow` で解消
- parquet の `video.bytes` から `data/work/input_0000.mp4` を復元
- 初回 `run.sh` 失敗原因:
  - `third_party/VACE/checkpoints/model.pth` が存在しない
- 暫定対策として identity fallback を追加
  - 重みが無い場合は入力フレームをそのまま出力フレームへコピー
- その後 `run.sh` は成功

## 3. 変更ファイル（今回の主対象）
- `src/model/vace_wrapper.py`
  - `identity_fallback` 実装
- `configs/base.yaml`
  - `model.identity_fallback: true` を有効化
- `docs/baseline_trial_log.md`
  - Trial 001-008 を時系列で追記

## 4. 重要ログ
- 詳細な試行履歴: `docs/baseline_trial_log.md`
- GroundingDINO 側の別件修正履歴: `docs/groundingdino_notebook_fix.md`

## 5. 次のPCでの再開手順（大きいGPU環境）
1. `/workspace/docker/vace/install_vace_library.sh` 実行済みであることを確認
2. VACE重みを配置
   - 想定先: `third_party/VACE/checkpoints/model.pth`（現在は未配置）
   - もしくは `configs/base.yaml` の `model_path` を実在パスへ更新
3. まずは現在の動作確認
   - `bash scripts/run.sh configs/base.yaml data/work/input_0000.mp4 "Make the scene more rainy"`
4. 実モデル検証に移行
   - `configs/base.yaml` の `model.identity_fallback` を `false` に戻す
   - VACEの実際の CLI に合わせて `src/model/vace_wrapper.py` の `_build_command` を修正
     - 現在の `inference.py --input_dir ...` 形式は VACE本家CLIと一致していない可能性が高い
5. 提出用生成
   - parquet 全件分の動画を復元して推論
   - 競技ページの提出形式（ファイル名・圧縮形式）に合わせてパッケージ化

## 6. ブロッカー
- 最大の未解決点: VACEチェックポイント未配置
- 次点: 提出フォーマット（Codabench 側の厳密仕様）を最終確認して自動化する必要あり

## 7. 補足
- 今回のベースライン成功は「提出パイプライン疎通」の確認が目的
- 品質向上は重み配置後に実施
